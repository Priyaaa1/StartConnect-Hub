import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
  botName:'InvestIQ',
  initialMessages: [createChatBotMessage(' Welcome to StartConnect -Hub 👋 How may I help you ?..')],
};
export default config; 